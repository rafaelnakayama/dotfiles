# ctrl+alt+n opens Ghostty, but that one is GNOME's "launch terminal"
# shortcut, set by install.sh; only the tab keybind belongs to Ghostty.
# Same physical keys as ctrl+cmd+n / cmd+n on macOS.
keybind = alt+n=new_tab
